---
name: whispr-testing
description: "Use when writing, locating, or running tests in whispr-messenger, mocking native modules, or checking coverage thresholds. Examples: \"Write a test for ChatScreen\", \"Where do tests go?\", \"Run coverage\", \"Mock expo-image-picker\", \"How do I mock fetch?\""
---

# Testing — whispr-messenger

## When to Use

- "Write a test for `<Component>`"
- "Where should this test go?"
- "Run the tests" / "Run coverage"
- "Mock `<expo module>` / `react-native-reanimated` / `fetch`"
- "Will this drop coverage below the threshold?"

## Test layout

| Kind | Location | Pattern |
|------|----------|---------|
| Unit | `src/<dir>/__tests__/` next to the source module | `*.test.ts` or `*.test.tsx` |
| Snapshots | `src/<dir>/__tests__/__snapshots__/` (auto-generated by Jest) | `*.snap` |
| E2E (web) | `e2e/` | Playwright |

For `src/foo/bar/Baz.tsx`, place its test at `src/foo/bar/__tests__/Baz.test.tsx`. Jest config (`package.json`) restricts test discovery to `src/**/__tests__/**` — tests placed elsewhere will not run.

## Rules

- **Test behaviour, not implementation.** Assert on observable outcomes (rendered output, navigation calls, state changes) rather than internal call sequences.
- Mock all network calls and native modules — never hit real services.
- Use React Native Testing Library (`@testing-library/react-native`) for component tests.

## Run tests

```bash
# All tests
npm test -- --watchAll=false

# Specific pattern
npm test -- --watchAll=false --testPathPattern="ChatScreen"

# Full suite with coverage report (writes coverage/ and runs threshold check)
npm run test:coverage
```

All tests must be green before committing.

## Coverage thresholds

CI enforces these minimums via `scripts/check-coverage.js`:

| Metric     | Min  |
|------------|------|
| Statements | 55%  |
| Branches   | 65%  |
| Functions  | 48%  |
| Lines      | 55%  |

PRs must keep coverage above these bars. The script consumes
`coverage/coverage-summary.json`, produced by `npm run test:coverage`.

## Test patterns — reference files

Reach for these reference files when writing new tests:

| Kind | Reference |
|------|-----------|
| API service with `fetch` + `TokenService` | `src/services/contacts/__tests__/contactsApi.test.ts`, `src/services/groups/__tests__/api.test.ts` |
| Component with `expo-image-picker` / `expo-haptics` / `LinearGradient` | `src/components/Chat/__tests__/CameraCapture.test.tsx`, `src/components/Chat/__tests__/MessageInput.test.tsx` |
| Modal / sheet wizard with multi-step state | `src/components/Chat/__tests__/ReportMessageSheet.test.tsx`, `src/components/Chat/__tests__/BlockedImageAppealModal.test.tsx` |
| Component reading Zustand stores + context | `src/components/Chat/__tests__/ConversationItem.test.tsx` |
| Reanimated mock | `src/components/Chat/__tests__/MessageBubble.test.tsx` (inline `jest.mock("react-native-reanimated", …)`) |

## Shared helpers

Shared helpers live in `src/__test-utils__/` — prefer `installFetchMock` +
`mockResponse` from `mockFactories.ts` over hand-rolling fetch mocks.

## Checklist

```
- [ ] Test file at src/<dir>/__tests__/<Module>.test.tsx
- [ ] Network calls and native modules mocked
- [ ] Assertions on observable outcomes, not call sequences
- [ ] All tests green (npm test -- --watchAll=false)
- [ ] Coverage above thresholds (npm run test:coverage)
```
